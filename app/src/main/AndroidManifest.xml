<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher3"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher3_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!-- 🚀 Main launcher screen -->
        <activity
            android:name=".ChatbotActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>

        <!-- ✅ Upload image screen -->
        <activity android:name=".UploadActivity" />

        <!-- ✅ Task ready message screen -->
        <activity android:name=".TaskReadyActivity" />

        <!-- ✅ Task in progress (loading + timer) -->
        <activity android:name=".TaskInProgressActivity" />

        <activity android:name=".UploadNewClean" />

        <activity android:name=".MainActivity" />


    </application>

</manifest>